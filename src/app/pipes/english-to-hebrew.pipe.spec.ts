import { EnglishToHebrewPipe } from './english-to-hebrew.pipe';

describe('EnglishToHebrewPipe', () => {
  it('create an instance', () => {
    const pipe = new EnglishToHebrewPipe();
    expect(pipe).toBeTruthy();
  });
});
